[tasks.build]
description = "Build the static site"
run = [
  "hugo",
  "cp _redirects public/_redirects",
]

[tasks.dev]
description = "Run the Hugo development server"
run = 'hugo server --buildDrafts --bind "0.0.0.0"'

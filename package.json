{
  "name": "alexplescan.com",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:alexpls/alexplescan.com.git",
  "author": "Alex Plescan <alexpls@fastmail.com>",
  "license": "MIT",
  "devDependencies": {
    "ts-loader": "^6.0.1",
    "typescript": "^3.4.5",
    "webpack": "^4.31.0",
    "webpack-cli": "^3.3.2"
  },
  "scripts": {
    "build": "yarn build:ts && yarn build:rb",
    "build:rb": "JEKYLL_ENV=production bundle exec jekyll build --config _config.yml",
    "build:ts": "webpack",
    "build:watch:rb": "bundle exec jekyll serve",
    "build:watch:ts": "webpack --watch",
    "deploy": "AWS_PROFILE=alexpls aws s3 cp _site/ s3://alexplescan.com/ --recursive"
  },
  "dependencies": {
    "vanilla-lazyload": "^12.0.0"
  }
}
